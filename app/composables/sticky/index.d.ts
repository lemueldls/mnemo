export interface StickyNote {
  id: string;
  title: string;
  x: number;
  y: number;
  rx: number;
  ry: number;
  width: number;
  height: number;
  // datetime: [number, number, number, number, number];
}
