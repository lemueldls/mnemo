name: "🖥️ Desktop (publish)"

on:
  release:
    types:
      - published

jobs:
  publish:
    permissions:
      id-token: write
      attestations: write
      contents: write
      actions: read
    uses: ./.github/workflows/desktop.yml
    secrets: inherit
    with:
      publish: true
